<%= form_with model: @article do |f| %>
  <div class="mt-3">
    <%= f.label :title %>
    <%= f.text_field :title, class: "input input-bordered ml-2" %>
  </div>
  <%= f.submit "登録", class: 'btn btn-primary mt-4' %>
<% end %>